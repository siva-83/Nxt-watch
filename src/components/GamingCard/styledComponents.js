import styled from 'styled-components'

export const GameHead = styled.p`
  font-family: 'Roboto';
  font-weight: bold;
  font-size: 10px;
  color: ${props => (props.s ? '#1e293b' : '#ffffff')};
`
export const GamePara = styled.p`
  font-family: 'Roboto';
  font-weight: bold;
  font-size: 10px;
  color: ${props => (props.s ? '#1e293b' : '#ffffff')};
`
